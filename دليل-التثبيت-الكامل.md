# 📱 دليل التثبيت الكامل - تطبيق القرآن الكريم

## 🎯 نظرة عامة

هذا الدليل يشرح بالتفصيل كيفية تثبيت وتشغيل وبناء تطبيق القرآن الكريم على هاتفك.

---

## المحتويات

1. [المتطلبات الأساسية](#المتطلبات-الأساسية)
2. [التثبيت للمطورين](#التثبيت-للمطورين)
3. [تشغيل التطبيق](#تشغيل-التطبيق)
4. [بناء ملف APK](#بناء-ملف-apk)
5. [التثبيت على الهاتف](#التثبيت-على-الهاتف)
6. [حل المشاكل](#حل-المشاكل)

---

## المتطلبات الأساسية

### للمطورين (لبناء التطبيق):

#### 1. Node.js
```bash
# تحميل من:
https://nodejs.org

# التحقق من التثبيت:
node --version
# يجب أن يكون 18 أو أعلى

npm --version
```

#### 2. Git (اختياري)
```bash
# تحميل من:
https://git-scm.com

# التحقق:
git --version
```

#### 3. لبناء APK محلياً (اختياري):

**Android Studio**
- تحميل من: https://developer.android.com/studio
- تثبيت Android SDK
- إعداد متغيرات البيئة

**Java JDK 17+**
```bash
# التحقق:
java -version
```

### للمستخدمين (لتثبيت APK فقط):

- ✅ هاتف Android 5.0 أو أعلى
- ✅ مساحة حرة ~100 MB
- ✅ اتصال إنترنت (للآيات والبحث)

---

## التثبيت للمطورين

### الخطوة 1: الحصول على الكود

```bash
# إذا كان المشروع على Git:
git clone [repository-url]
cd project

# أو إذا كان عندك مجلد المشروع:
cd /path/to/project
```

### الخطوة 2: تثبيت المكتبات

```bash
# داخل مجلد المشروع:
npm install
```

**الوقت المتوقع**: 2-5 دقائق

**ماذا يحدث؟**
- تحميل جميع المكتبات المطلوبة
- إنشاء مجلد node_modules
- تجهيز البيئة

**الخرج المتوقع**:
```
added 750+ packages in 2m
```

### الخطوة 3: التحقق من التثبيت

```bash
# تحقق من صحة package.json
node -e "console.log('✅ Node.js يعمل')"

# تحقق من البيانات
node -e "const s = require('./data/surahs.json'); console.log('✅ تم تحميل', s.length, 'سورة')"
```

**الخرج المتوقع**:
```
✅ Node.js يعمل
✅ تم تحميل 114 سورة
```

---

## تشغيل التطبيق

### الطريقة 1: باستخدام Expo Go (الأسهل)

#### أ) على الكمبيوتر:

```bash
npm start
```

**سيظهر**:
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code with Expo Go (Android) or Camera (iOS)
```

#### ب) على الهاتف:

1. **ثبت Expo Go** من Play Store:
   ```
   https://play.google.com/store/apps/details?id=host.exp.exponent
   ```

2. **افتح التطبيق**

3. **امسح QR Code** الظاهر على الكمبيوتر

4. **انتظر التحميل** (~10-30 ثانية)

5. **استمتع بالتطبيق!** 🎉

### الطريقة 2: على المحاكي

```bash
# تأكد أن المحاكي يعمل
# ثم:
npm run android
```

**المتطلبات**:
- Android Studio مثبت
- محاكي Android جاهز

---

## بناء ملف APK

### ⭐ الطريقة 1: EAS Build (موصى بها جداً)

هذه الطريقة **الأسهل والأفضل** - تبني في السحابة!

#### الخطوة 1: تثبيت EAS CLI

```bash
npm install -g eas-cli
```

#### الخطوة 2: إنشاء حساب Expo

زر: https://expo.dev/signup
- أدخل بريدك الإلكتروني
- اختر كلمة مرور
- فعّل الحساب

#### الخطوة 3: تسجيل الدخول

```bash
eas login
```

أدخل:
- Email: (بريدك)
- Password: (كلمة المرور)

#### الخطوة 4: بناء APK

```bash
eas build -p android --profile preview
```

**ماذا يحدث؟**
1. يرفع الكود إلى خوادم Expo
2. يبني APK في السحابة
3. يعطيك رابط للتحميل

**الوقت المتوقع**: 10-20 دقيقة

**الخرج النهائي**:
```
✔ Build finished.
🚀 Install the build on a device:
   Download: https://expo.dev/artifacts/[build-id]
```

#### الخطوة 5: تحميل APK

1. انسخ الرابط من الخرج
2. افتحه في المتصفح
3. حمّل ملف APK (~30-50 MB)

---

### 🔧 الطريقة 2: Build محلي

هذه الطريقة أصعب قليلاً لكن لا تحتاج حساب Expo.

#### المتطلبات:

1. **Android Studio مثبت**
2. **Android SDK مُعد**
3. **JDK 17+ مثبت**

#### إعداد متغيرات البيئة:

**على Linux/Mac:**
```bash
export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin

# أضفهم في ~/.bashrc أو ~/.zshrc للدوام
```

**على Windows (PowerShell):**
```powershell
$env:ANDROID_HOME = "C:\Users\[YourName]\AppData\Local\Android\Sdk"
$env:PATH += ";$env:ANDROID_HOME\platform-tools"
```

#### البناء:

**الطريقة التلقائية:**
```bash
./build-apk.sh
```

**الطريقة اليدوية:**
```bash
# 1. إعداد Android
npx expo prebuild --platform android --clean

# 2. بناء APK
cd android
./gradlew assembleRelease
cd ..

# 3. APK سيكون في:
# android/app/build/outputs/apk/release/app-release.apk
```

**الوقت المتوقع**: 5-15 دقيقة (أول مرة)

**النتيجة**:
```
BUILD SUCCESSFUL in 8m 32s
```

#### نسخ APK:

```bash
cp android/app/build/outputs/apk/release/app-release.apk ./quran-app.apk
```

---

## التثبيت على الهاتف

### الطريقة 1: نقل مباشر

#### أ) عبر USB:

1. **وصّل الهاتف بالكمبيوتر** عبر كابل USB

2. **انسخ ملف APK** إلى الهاتف:
   ```bash
   # ابحث عن:
   app-release.apk
   أو
   quran-app.apk
   ```

3. **على الهاتف**:
   - افتح مدير الملفات
   - ابحث عن الملف
   - اضغط عليه

4. **قد يظهر تحذير**:
   - "تثبيت من مصادر غير معروفة"
   - اضغط "السماح" أو "تفعيل"

5. **اضغط "تثبيت"**

6. **انتظر** (10-30 ثانية)

7. **اضغط "فتح"** 🎉

#### ب) عبر Email/WhatsApp:

1. **أرسل ملف APK** لنفسك:
   - عبر Gmail
   - أو WhatsApp
   - أو أي طريقة نقل

2. **على الهاتف**:
   - افتح الرسالة
   - حمّل الملف
   - اضغط عليه للتثبيت

### الطريقة 2: عبر ADB

```bash
# تأكد أن USB Debugging مفعّل على الهاتف

# تحقق من الاتصال:
adb devices

# ثبت APK:
adb install app-release.apk

# أو:
adb install -r app-release.apk  # للتحديث
```

---

## استخدام التطبيق

### 1️⃣ الشاشة الرئيسية

عند فتح التطبيق:
- ✅ سترى البسملة في الأعلى
- ✅ حقل بحث للسور
- ✅ زر "البحث المتقدم"
- ✅ قائمة بجميع السور (114 سورة)

**لاختيار سورة**:
- اضغط على أي سورة من القائمة
- سيفتح صفحة الآيات

**للبحث في القائمة**:
- اكتب في حقل البحث
- السور ستُصفى تلقائياً

### 2️⃣ صفحة السورة

بعد اختيار سورة:
- ✅ معلومات السورة في الأعلى
- ✅ البسملة (إلا سورة التوبة)
- ✅ جميع آيات السورة
- ✅ أرقام الآيات واضحة

**للقراءة**:
- مرر لأعلى/أسفل
- الخط واضح وكبير
- النص مريح للعين

**للرجوع**:
- اضغط السهم في الأعلى
- أو زر الرجوع في الهاتف

### 3️⃣ البحث المتقدم

من الشاشة الرئيسية:
- اضغط "البحث المتقدم"
- اكتب الكلمة المراد البحث عنها
- اضغط "بحث"
- ستظهر جميع النتائج

**للانتقال للسورة**:
- اضغط على أي نتيجة
- سينتقل لصفحة السورة مباشرة

---

## حل المشاكل

### مشكلة: npm install يفشل

**الحل**:
```bash
# احذف الكاش
rm -rf node_modules
rm package-lock.json

# أعد التثبيت
npm cache clean --force
npm install
```

### مشكلة: expo start لا يعمل

**الحل 1**:
```bash
# ثبت expo-cli
npm install -g expo-cli

# جرب مرة أخرى
npx expo start --clear
```

**الحل 2**:
```bash
# استخدم tunnel بدل LAN
npx expo start --tunnel
```

### مشكلة: QR Code لا يعمل

**الحل**:
- تأكد أن الهاتف والكمبيوتر على نفس الشبكة
- جرب tunnel mode:
  ```bash
  npx expo start --tunnel
  ```
- أو استخدم رابط مباشر يظهر في Console

### مشكلة: ANDROID_HOME غير معرف

**الحل**:
```bash
# ابحث عن مجلد SDK:
# على Linux/Mac:
find ~ -name "Sdk" 2>/dev/null | grep Android

# على Windows:
dir /s /b "Android\Sdk"

# بعدها عرّف المتغير:
export ANDROID_HOME=/path/to/Android/Sdk
```

### مشكلة: gradlew assembleRelease يفشل

**الحل**:
```bash
cd android

# نظف
./gradlew clean

# جرب مرة أخرى
./gradlew assembleRelease --stacktrace

cd ..
```

### مشكلة: ملف APK لا يثبت على الهاتف

**الأسباب المحتملة**:

1. **الملف تالف**:
   - تحقق من حجم الملف (يجب ~30-50 MB)
   - أعد البناء

2. **الصلاحيات**:
   - فعّل "السماح من مصادر غير معروفة"
   - Settings → Security → Unknown Sources

3. **إصدار Android**:
   - التطبيق يحتاج Android 5.0+
   - تحقق من إصدار هاتفك

4. **مساحة غير كافية**:
   - احذف ملفات غير مهمة
   - التطبيق يحتاج ~100 MB

### مشكلة: التطبيق لا يحمل الآيات

**الحل**:
- ✅ تأكد من اتصال الإنترنت
- ✅ جرب سورة أخرى
- ✅ أغلق وافتح التطبيق مرة أخرى
- ✅ تحقق من API: https://api.alquran.cloud/v1/surah/1

### مشكلة: البحث لا يعمل

**الحل**:
- ✅ تأكد من اتصال الإنترنت
- ✅ جرب كلمة بحث مختلفة
- ✅ تأكد أن الكلمة بالعربية
- ✅ انتظر قليلاً (قد يستغرق 2-3 ثواني)

---

## نصائح وإرشادات

### للمطورين:

1. **استخدم EAS Build**:
   - أسهل وأسرع
   - لا يحتاج إعدادات معقدة
   - موثوق

2. **نظف البروجكت بانتظام**:
   ```bash
   npm cache clean --force
   rm -rf node_modules
   npm install
   ```

3. **حافظ على التحديثات**:
   ```bash
   npm update
   expo upgrade
   ```

### للمستخدمين:

1. **اتصال الإنترنت**:
   - مطلوب لتحميل الآيات
   - قائمة السور تعمل بدون إنترنت

2. **البطارية**:
   - التطبيق لا يستهلك بطارية كثيرة
   - يمكن إغلاق التطبيقات الأخرى

3. **التحديثات**:
   - احتفظ بملف APK لإعادة التثبيت
   - التحديثات المستقبلية ستكون بنفس الطريقة

---

## الأسئلة الشائعة

### هل التطبيق مجاني؟
✅ نعم، مجاني 100% بدون إعلانات

### هل يحتاج إنترنت؟
⚠️ نعم، للآيات والبحث فقط
✅ قائمة السور تعمل بدون إنترنت

### ما حجم التطبيق؟
📦 APK: ~30-50 MB
📱 بعد التثبيت: ~60-80 MB

### هل يعمل على جميع هواتف Android؟
✅ نعم، Android 5.0 أو أعلى

### هل البيانات دقيقة؟
✅ نعم، من مصدر موثوق (AlQuran Cloud API)

### هل يمكن مشاركة التطبيق؟
✅ نعم، شارك ملف APK مع العائلة والأصدقاء

### هل يحتاج تسجيل دخول؟
✅ لا، لا يحتاج أي تسجيل

### ما الصلاحيات المطلوبة؟
✅ الإنترنت فقط، لا صلاحيات أخرى

---

## الخلاصة

**التطبيق جاهز 100% للاستخدام!**

### ما تم:
✅ تطبيق كامل ومتكامل
✅ 114 سورة مع جميع الآيات
✅ بحث متقدم في القرآن
✅ واجهة عربية جميلة
✅ سهل التثبيت والاستخدام

### الخطوة التالية:
1. اختر طريقة بناء APK
2. ابن الملف
3. ثبت على هاتفك
4. استمتع بقراءة القرآن الكريم! 📱✨

---

**تقبل الله منا ومنكم**
**جزاكم الله خيراً**

**للمساعدة: راجع ملفات README.md و DEPLOYMENT.md**

🎉 **بالتوفيق!** 🎉
