# Quran Native (React Native)

تطبيق القرآن الكريم مبني باستخدام **React Native** (نظام Expo) ويوفر تجربة سلسة باللغة العربية مع إمكانية تصفح السور، قراءة الآيات، والبحث الذكي داخل المصحف الشريف.

## المميزات الرئيسية

- 📖 **قائمة السور كاملة** مع عدد الآيات ونوع السورة (مكية / مدنية).
- 📜 **عرض الآيات** بتنسيق عربي واضح مع تمييز الآية المختارة عند الانتقال إليها.
- 🔍 **بحث فوري في جميع آيات القرآن** مع إزالة التشكيل للحصول على نتائج دقيقة.
- 🌙 تصميم أنيق يدعم اتجاه الكتابة من اليمين إلى اليسار وتجربة استخدام سلسة على الهواتف الذكية.

## المتطلبات المسبقة

- Node.js 18 أو أحدث.
- npm أو yarn.
- حساب Expo (اختياري فقط عند إنشاء ملف APK جاهز عبر EAS).

## تشغيل التطبيق محليًا

```bash
npm install
npm run start
```

سيقوم الأمر `npm run start` بتشغيل Expo Dev Server، ويمكنك اختيار تشغيل التطبيق على:

- محاكي Android (`a` داخل واجهة Expo CLI)
- جهاز حقيقي عبر تطبيق Expo Go

## إنشاء ملف APK للتثبيت على Android

> **ملاحظة:** المشروع يستخدم Expo (Managed Workflow). هناك طريقتان رئيسيتان للحصول على APK:

### 1. استخدام EAS Build (موصى به)

1. ثبت أداة EAS:
   ```bash
   npm install -g eas-cli
   ```
2. سجّل الدخول إلى Expo/EAS:
   ```bash
   eas login
   ```
3. أنشئ ملف إعدادات (سيتم توليده تلقائيًا عند أول تشغيل):
   ```bash
   eas build:configure
   ```
4. نفّذ عملية البناء:
   ```bash
   eas build -p android --profile preview
   ```
5. بعد انتهاء البناء، ستحصل على رابط لتحميل ملف الـ APK (أو AAB حسب الإعدادات).

### 2. إنشاء APK محليًا (خارج Expo Go)

1. نفّذ عملية التحويل إلى مشروع React Native تقليدي (Prebuild):
   ```bash
   npx expo prebuild --platform android
   ```
2. انتقل إلى مجلد `android` وشغّل أمر البناء:
   ```bash
   cd android
   ./gradlew assembleRelease
   ```
3. ستجد ملف الـ APK في المسار: `android/app/build/outputs/apk/release/app-release.apk`

> **ملاحظة:** بعد تنفيذ `expo prebuild` سيتم إنشاء مجلدات `android` و `ios`. تأكد من إضافتها إلى نظام التحكم بالنسخ إذا كنت بحاجة إليها، أو أضفها إلى `.gitignore` (موجودة بالفعل في هذا المشروع).

## بنية المشروع

```
.
├── App.tsx
├── app.json
├── assets/
├── src/
│   ├── components/
│   ├── context/
│   ├── screens/
│   ├── services/
│   ├── theme/
│   ├── types/
│   └── utils/
└── README.md
```

- **context/QuranContext.tsx**: جلب بيانات المصحف من API وبناء فهرس البحث.
- **screens/HomeScreen.tsx**: عرض قائمة السور والبحث في الآيات.
- **screens/SurahScreen.tsx**: عرض آيات السورة المحددة مع تمييز الآية المطلوبة.
- **services/api.ts**: استدعاءات واجهات API الخاصة بالقرآن.
- **utils/arabic.ts**: دوال مساعدة للتعامل مع النصوص العربية وإزالة التشكيل.

## تحسينات مقترحة مستقبلية

- دعم الوضع الداكن.
- إضافة استماع صوتي لتلاوة الآيات.
- حفظ السور المفضّلة محليًا.
- العمل دون اتصال من خلال تخزين البيانات في الجهاز.

## حقوق البيانات

يتم جلب الآيات من واجهة API العامة [api.alquran.cloud](https://api.alquran.cloud/). تأكد من مراجعة شروط الاستخدام إذا رغبت في استخدامها في تطبيق إنتاجي.

---

بالتوفيق، ونسأل الله أن يجعل هذا العمل خالصًا لوجهه الكريم.
